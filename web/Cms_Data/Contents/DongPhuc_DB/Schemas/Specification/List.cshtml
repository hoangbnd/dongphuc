<h3>Specification</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Name"], "Specification/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>